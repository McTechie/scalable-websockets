export enum Event {
  CONNECTION = 'connection',
  MESSAGE = 'event:message',
  LISTEN = 'event:listen',
  DISCONNECT = 'disconnect',
}

export enum Channel {
  MESSAGES = 'MESSAGES',
}

export enum Topic {
  MESSAGES = 'MESSAGES',
}
